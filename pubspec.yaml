name: rive_trigger_repro
description: "Minimal reproducer for a Rive Flutter ViewModelInstanceTrigger bug."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  rive: ^0.14.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/repro.riv
